#include <stdio.h>

/**
 * main - Entry point
 *
 * prints numbers in asc order with printf
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	printf("\n");
	return (0);
}
