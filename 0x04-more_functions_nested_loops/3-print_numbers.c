#include "main.h"

/**
 * print_numbers - function that multiplies two int numbers
 *
 * Return: void return only prints
 */
void print_numbers(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
	{
		_putchar(i);
	}
	_putchar(10);
}
