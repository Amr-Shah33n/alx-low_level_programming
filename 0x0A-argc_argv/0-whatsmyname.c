#include <stdio.h>

/**
 * main - prints program's name
 * @argc: argument count
 * @argv: array of input arguments
 * Return: 0 on success.
 **/
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
